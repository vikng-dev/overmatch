<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Steam injects its overlay/API dylib via DYLD_INSERT_LIBRARIES, which the
      hardened runtime blocks by default. These two entitlements re-enable that
      (per Steamworks macOS docs). Harmless for the standalone GitHub build, so we
      keep one signing lane that serves both GitHub Releases and the Steam depot.
      Note: do NOT add com.apple.security.app-sandbox — it is incompatible with Steam.
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
