# NaN/Placeholder rollback bug — lightyear 0.28.0 example & book research

Source: lightyear repo clone at tag 0.28.0 (`scratchpad/lightyear-src`). All paths relative to the clone root. All line numbers from actual reads of this clone.

## Q1: `examples/deterministic_replication` — how entities get `DeterministicPredicted`, and what they carry at that moment

There are **three** decoration sites; in every one of them the entity has real (never placeholder) `Position` at decoration time, and physics components arrive **in the same `spawn`/`insert` call** as `DeterministicPredicted` or before it — never after.

1. **Ball (shared, both peers)** — `examples/deterministic_replication/src/shared.rs:150-176` (`spawn_ball`): one `commands.spawn((...))` containing `Position::default()` (explicit, shared.rs:157), `PhysicsBundle::ball()` (Collider + ColliderDensity + RigidBody + Restitution, protocol.rs:17-33), and `DeterministicPredicted { skip_despawn: true, ..default() }` (shared.rs:161-164). Position exists with a real value from the entity's very first tick.

2. **Server player** — `examples/deterministic_replication/src/server.rs:134-148` (`handle_connected`): one `commands.spawn((...))` with `player_bundle(remote_id.0)` — which contains an explicit `Position::from(Vec2::new(-50.0, y))` (shared.rs:178-187) — plus `DeterministicPredicted { skip_despawn: true, ..default() }` (server.rs:144-147). The in-code comment at server.rs:139-143 is load-bearing: *"`skip_despawn: true` — the player is spawned by a non-deterministic event... Rollback must not despawn the entity, and `enable_rollback_after: N` prevents restoring history for ticks before the entity existed on this peer."*

3. **Client player activation (the closest analog to our async bind)** — `examples/deterministic_replication/src/client.rs:72-110` (`activate_physics_when_bundle_lands`, an `On<CatchUpSnapshotReady>` observer, registered client.rs:29). It inserts `PhysicsBundle::player()` + `DeterministicPredicted { skip_despawn: true }` onto an already-existing replicated entity — i.e., physics IS added after spawn here. But two things differ from our pattern: (a) the entity already carries authoritative replicated `Position`/`Rotation` from the catch-up snapshot **before** the collider is inserted (the observer only fires after `CatchUpSnapshotReady`, i.e. "after all gated state arrives", client.rs:66-71; the server replicates `Position` etc. per protocol.rs:115-136); (b) the collider bundle and `DeterministicPredicted` land **atomically in one insert** (client.rs:99-109), so there is never a tick where a physics component exists on the entity without the rollback-grace marker.

**Do decorated entities carry avian `Position`/`Rotation`?** Yes — `Position` always explicitly (shared.rs:157, shared.rs:182), and both `Position` and `Rotation` are registered for `local_rollback` with confirmed writes, custom hashes, interpolation and correction (protocol.rs:115-129). `Rotation` is never explicitly inserted in spawn bundles in this example — it comes in via avian2d required components (UNCERTAIN whether avian2d 0.x's `Rotation` default is a finite identity vs. a placeholder here; avian source is not in this clone. In avian2d the example never crashes on it, and in the client activation path `Rotation` is replicated state before the collider lands).

**How the example avoids restoring placeholder history — the actual mechanism (lightyear core, not example code):**
- `DeterministicPredicted` has a built-in grace field: `enable_rollback_after: u8`, default **20 ticks** — `crates/replication/prediction/src/rollback.rs:255-275`.
- Its `on_add` hook records `(decoration_tick + enable_rollback_after, entity)` into `PredictionManager.deterministic_skip_despawn` — rollback.rs:277-300 (note rollback.rs:282-291: the hook **silently no-ops if the `PredictionResource`/`PredictionManager` doesn't exist yet** — the protection is not applied in that case).
- When any rollback fires, the tail of `check_rollback` inserts **`DisableRollback`** on every skip-despawn entity whose protection tick is still in the future (`rollback_tick < protection_tick`), and removes it from entities whose window has passed — rollback.rs:767-812 (insert at :799, removal at :807).
- `RollbackSystems::Check → RemoveDisable → Prepare → Rollback → EndRollback` are `.chain()`ed (rollback.rs:121-132), so the `DisableRollback` command applies before `prepare_rollback` runs.
- `prepare_rollback<C>` queries `Without<DisableRollback>` (rollback.rs:883-891), so protected entities are **entirely skipped from restore**. During rollback the whole entity is also hidden from all queries via `DisabledDuringRollback` (rollback.rs:303-311, 1103-1109) and un-hidden at rollback end (rollback.rs:1145-1147).
- Restore semantics: `prepare_rollback` restores `predicted_history.get_state(rollback_tick)` (rollback.rs:932-936), and `get_state` is **at-or-before** semantics (`crates/core/core/src/history_buffer.rs:160-168`). So if junk is ever recorded at an entity's first tick, any rollback landing at-or-after that tick (and before the next history entry) restores the junk — unless the `DisableRollback` grace window covers it. If no history entry exists at-or-before the rollback tick, the component is left untouched (rollback.rs:964-974), so an *empty* history is safe; a *junk-seeded* history is not.

Also relevant: the example adds `FrameInterpolate<Position/Rotation>` via an `On<Add, DeterministicPredicted>` observer (shared.rs:96-116) specifically so post-rollback `Position` isn't clobbered by stale `Transform` at FixedUpdate start (shared.rs:29-37) — a reminder that with `PhysicsTransformPlugin` disabled (shared.rs:46-55) the examples are explicit about who owns transform↔position sync.

## Q2: Does any example/demo add colliders/physics to a predicted entity AFTER spawn?

**No example uses async asset-driven collider binding.** `grep` for `ColliderConstructor`, `ColliderConstructorHierarchy`, `SceneRoot`, `.glb`, `.gltf`, `GltfAssetLabel` across `examples/` and `demos/` returns **zero hits**. No example loads a rig/scene and binds colliders when the asset resolves. Our ~1s-deferred glb bind has **no reference precedent** in this repo.

What DOES exist is the "blueprint pattern": physics bundles inserted onto a predicted entity **at the moment it becomes Predicted** (same frame or the next, via `Added<Predicted>` queries or `On<Add, ...>` observers), when the entity already carries authoritative replicated `Position` (and `Rotation` where registered):

- `examples/avian_3d_character/src/client.rs:65-79` — `handle_new_character`: `Added<Predicted> + With<CharacterMarker>` → insert `CharacterPhysicsBundle` (Collider/RigidBody/LockedAxes/Friction, **no** Position/Rotation — those are replicated per protocol.rs:88-100). Similar: floors client.rs:112-122 (`Added<Remote>`), blocks client.rs:125-135 (`Added<Predicted>`).
- `examples/avian_physics/src/client.rs:41-51` — `add_ball_physics` (`On<Add, BallMarker>`, if Predicted) and client.rs:86-102 `handle_predicted_spawn` (`On<Add, (PlayerId, Predicted)>` → `PhysicsBundle::player()`).
- `examples/projectiles/src/client.rs:53-61,67-80` — Collider+RigidBody inserted on predicted/interpolated players on add; client.rs:82-100 `handle_deterministic_spawn` inserts `player_bundle(...)` (which itself contains explicit `Position::from_xy` AND `Rotation::default()` alongside the Collider — shared.rs:1935-1954) **plus** `DeterministicPredicted { skip_despawn: true }` in one insert.
- `demos/spaceships/src/client.rs:61-81` — `add_bullet_physics`: `With<BulletMarker>, Without<Collider>` → insert `PhysicsBundle::bullet()` on predicted bullets / sensor collider on interpolated ones.
- `examples/deterministic_replication/src/client.rs:72-110` — the catch-up activation described in Q1 (physics + `DeterministicPredicted` atomic, after authoritative Position/Rotation landed).

Common invariants across every site: (1) the insert happens within one frame of the entity becoming Predicted/replicated — never seconds later mid-gameplay; (2) `Position` (the prediction-registered transform state) is already present with a real value BEFORE or ATOMICALLY WITH the collider; (3) the insert targets the **root** entity only; (4) where `DeterministicPredicted` is involved, it lands in the same `insert`/`spawn` as the physics bundle.

## Q3: `DisableRollback` usage in `avian_3d_character` and `projectiles`; spawn bundle completeness

### avian_3d_character
- `examples/avian_3d_character/src/renderer.rs:181-199` — `disable_projectile_rollback`: query `With<Predicted>, With<ProjectileMarker>, Without<DisableRollback>` → `commands.entity(proj).insert(DisableRollback)`. Registered in `Last` (renderer.rs:48) with the comment *"We disable rollbacks for projectiles after the initial rollbacks which brings them to the predicted timeline"*; setup intent at renderer.rs:29-32: *"we use ReplicateOnce and DisableRollback to stop replicating any packets for these bullets"*.
- **Lifecycle: one-way.** No `.remove::<DisableRollback>()` exists anywhere in `examples/` or `demos/` (grep). Once a projectile has it, it is permanently excluded from rollback restore (`prepare_rollback` filters `Without<DisableRollback>`, `crates/replication/prediction/src/rollback.rs:890`) and hidden during rollback execution via `DisabledDuringRollback` (rollback.rs:1103-1109, removed at rollback end :1145-1147). The only removal of `DisableRollback` in the whole repo is lightyear-internal, for `DeterministicPredicted` skip-despawn entities whose grace window expired (rollback.rs:787, :807).
- Purpose: the projectile is predicted-spawned and needs exactly one initial state sync onto the predicted timeline; after its first predicted frame, its trajectory is fully deterministic locally, so rollback restore would only cause it to "jump" — hence permanent opt-out. The `Last`-schedule timing guarantees at least one full frame (including any rollback that synced it) has completed.
- Spawn completeness: server projectile spawn (`examples/avian_3d_character/src/server.rs:92-109`) includes explicit `*position` AND `Rotation::default()` AND `LinearVelocity` in the bundle. Character spawn (server.rs:176-191) has explicit `Position(Vec3::new(x, 3.0, z))` but **no explicit Rotation** — Rotation arrives via avian required components (UNCERTAIN what initial value avian3d assigns there; avian source not in this clone). Floor/block (server.rs:116-131): explicit `Position::new(...)`, no explicit Rotation, both with colliders at spawn.

### projectiles
All five `DisableRollback` sites are **at spawn time, in the spawn bundle** of client-side visual/local-sim projectile entities (not predicted entities):
- `examples/projectiles/src/shared.rs:1480-1506` hitscan visual (:1502);
- shared.rs:1509-1550 linear bullet (:1545) — with the load-bearing comment at :1542-1544: *"The entity is not predicted, so we want to disable it during rollbacks, otherwise it will start jumping forward on rollbacks"*;
- shared.rs:1553-1602 shotgun pellets (:1596); shared.rs:1604-1648 physics projectile (:1642); shared.rs:1650-1687 homing missile (:1684).
- Every one of those bundles contains explicit `position` AND `rotation` (copied from the shooter's components at fire time) as the first two bundle members (e.g. shared.rs:1531-1532, :1581-1582, :1624-1625, :1668-1669). The physics projectile additionally has `Collider::circle` + `RigidBody::Dynamic` in the same bundle (:1627-1628).
- `player_bundle` (shared.rs:1935-1954): explicit `Position::from_xy(0.0, y)` AND `Rotation::default()` AND `Collider::rectangle` AND `RigidBody::Kinematic` — the complete transform+physics state in one bundle.
- Lifecycle: same as above — inserted once, never removed.

**Pattern summary:** examples use `DisableRollback` in two shapes: (a) permanent opt-out for locally-deterministic predicted projectiles after their first predicted frame (avian_3d_character), (b) at-spawn opt-out for non-predicted visual entities that merely carry `Position`/`Rotation` (prediction-registered types) so rollbacks never touch them (projectiles). Shape (b) is exactly the "cosmetic entity carrying prediction-registered components" hazard — the example's answer is to mark them `DisableRollback` from birth.

## Q4: RayCaster entities and child colliders under predicted bodies — ALL examples

**(a) `RayCaster`: zero occurrences.** grep for `RayCaster`/`ShapeCaster` across `examples/` and `demos/` finds nothing. All raycasting is done through the `SpatialQuery` system param (e.g. `examples/avian_3d_character/src/shared.rs:120,149`; `examples/projectiles/src/shared.rs:327,508`; `examples/fps/src/server.rs:121,180`) or `LagCompensationSpatialQuery` (`examples/fps/src/server.rs:16`; `examples/projectiles/src/server.rs:30`), which adds **no components to any entity**. No example ever has a `RayCaster`-component entity, so no example ever needed to answer whether such an entity carries explicit `Position`/`Rotation`. Our RayCaster-component-on-entity pattern is also off the reference map.

**(b) Child colliders under a predicted rigid body: none exist.** Every `with_children`/`ChildOf` use in `examples/` and `demos/` is cosmetic or UI: bullet visual meshes (`demos/spaceships/src/renderer.rs:455-465`), wall visual meshes (renderer.rs:610-619), entity name labels (`demos/spaceships/src/entity_label.rs:62,72`), UI nodes (`examples/auth/src/client.rs:164`; `examples/common/src/client_renderer.rs:73`, `examples/common/src/server_renderer.rs:66`). A grep for `Collider`/`RigidBody` inside `with_children` blocks returns zero hits. In every example, the collider lives on the **same entity** as the rigid body and the prediction markers. `projectiles`' `BulletOf` (shared.rs:1500,1546) is a custom relationship, not a transform/physics hierarchy. **No example anywhere attaches physics components to child entities of a predicted body.**

## Q5: book/ coverage of late-added components, history seeding, DeterministicPredicted, DisableRollback, avian caveats

The book at this tag (`book/src`) has **no chapter** on `DeterministicPredicted`, `DisableRollback`, `PredictionHistory` seeding, or prediction of late-added components — zero mentions of either identifier anywhere under `book/src`. The only sources of truth are the crate doc-comments (`crates/replication/prediction/src/rollback.rs:246-306`) and example usage (Q1/Q3). Keyword grep hits only four files:

- `book/src/concepts/advanced_replication/prediction.md:54` — explains WHY avian `Position` must be predicted (modified each tick by physics, not derivable within the tick). prediction.md:95-108 covers "Component added on predicted" / "Component added on confirmed" edge cases — but strictly as presence-agreement scenarios between confirmed/predicted pairs ("Status: added unit test. Need to reconfirm that it works", :86,:93,:100,:108), never about a late-added component's *initial value* being unsafe/placeholder. This is the closest the book comes to our scenario, and it does not cover it.
- `book/src/concepts/advanced_replication/prespawning.md:55-57` — PredictionHistory is created for prespawned entities in a dedicated `FixedUpdate::SpawnHistory` step, explicitly "to not rollback immediately when we get the corresponding server entity". prespawning.md:73-78 rollback edge cases: *"if the prespawned entity didn't exist at the rollback tick, we despawn it; if a component didn't exist at the rollback tick, we remove it"* — the framework's model for late-added components on prespawned entities is REMOVE-on-rollback, not restore-junk. This is the book's only statement about rollback of late-added components.
- `book/src/concepts/bevy_integration/system_order.md:54-56,74-76,97` — schedule diagrams: `SpawnPredictionHistory → SpawnPredictionHistoryFlush → CheckRollback` (PreUpdate) and `UpdatePredictionHistory → IncrementRollbackTick` (FixedPostUpdate). The book's mental model assumes all history-eligible components exist before the tick's physics step; it has no accommodation for components added mid-lifetime by an unrelated async system.
- `book/src/concepts/advanced_replication/visual_interpolation.md:128-184` — avian usage section written against the old avian `SyncPlugin`/`SyncConfig` API (:167,:180,:184), i.e. stale relative to the 0.28 `PhysicsTransformPlugin` reality; strictly about transform-sync timing, never about a placeholder-value hazard.

**The real "avian integration caveats" documentation lives in the integration crate's module docs, not the book**: `crates/integration/avian/src/plugin.rs:1-31`:
- plugin.rs:15-18: *"Inserting `RigidBody` on an entity automatically inserts Position/Rotation/Transform on it"* — the required-components insertion path is known and warned about (in the context of Interpolated entities stuck at `Transform::default()`).
- plugin.rs:7-14: `Position` and `Rotation` may be added at different times on Interpolated entities; guidance is to act only when BOTH are present.
- plugin.rs:19-30: the canonical "disable PhysicsTransformPlugin + PhysicsInterpolationPlugin" snippet.

Nothing anywhere in book/ or crate docs covers seeding `PredictionHistory` for components added long after entity spawn. The framework itself acknowledges unresolved territory here: multiple TODOs in `crates/integration/avian/src/plugin.rs:161-182` about transform→position sync overwriting rollback results.

## Q6: protocol.rs scan — Position/Rotation prediction registration vs child/cosmetic entities

Registrations of avian `Position`/`Rotation` with prediction across all examples/demos:
- `.replicate().predict()`: `examples/avian_3d_character/src/protocol.rs:88-100`, `examples/avian_physics/src/protocol.rs:81-90`, `examples/projectiles/src/protocol.rs:356-368`, `examples/fps/src/protocol.rs:98-111`, `demos/spaceships/src/protocol.rs:205-217`, `examples/distributed_authority/src/protocol.rs:92` (Position only).
- `.replicate_once()` + `local_rollback::<Position/Rotation>().add_confirmed_write()` (deterministic): `examples/deterministic_replication/src/protocol.rs:115-129`.

**Where PredictionHistory actually attaches (framework mechanism):** `crates/replication/prediction/src/predicted_history.rs:237-265` — `add_prediction_history<C>` is an `On<Add, (C, Predicted, PreSpawned, DeterministicPredicted, CatchUpGated)>` observer: `PredictionHistory<C>` is inserted whenever registered component C coexists with one of those markers, **in either insertion order**. The buffer is inserted **empty** (`PredictionHistory::<C>::default()`, predicted_history.rs:308-310); authoritative `ConfirmedHistory` seeding only fires for `catchup_gated || predicted || prespawned` (predicted_history.rs:265) — **NOT for pure `DeterministicPredicted` entities**, whose first history entry therefore always comes from `update_prediction_history` (predicted_history.rs:94-123): the component's live value, change-detected, recorded at `FixedPostUpdate` in `PredictionSystems::UpdateHistory` — including the component's very first tick, whatever value it holds then. There is NO validity/finiteness filter in this path.

**Do examples have non-root child entities that could implicitly receive predicted Position/Rotation?** No hierarchy children carry physics at all (Q4b), so there is no structural opportunity for required-component propagation onto children. But `projectiles` has standalone cosmetic entities that DO carry predicted-registered `Position`/`Rotation` (the client-side bullet visuals, Q3) — its exclusion pattern is explicit: **`DisableRollback` in the spawn bundle from birth** (`examples/projectiles/src/shared.rs:1502,1545,1596,1642,1684`, rationale at shared.rs:1542-1544). These entities are never `Predicted`/`DeterministicPredicted`, so they never get a `PredictionHistory`; `DisableRollback` additionally hides them from all queries during rollback re-simulation via `DisabledDuringRollback` (rollback.rs:1103-1110, re-running `FixedMain` per rollback tick at rollback.rs:1115-1137).

How examples keep child/cosmetic entities out of prediction, in practice:
- **Never give them prediction-registered components at all** — cosmetic children get meshes + `Transform` only (`demos/spaceships/src/renderer.rs:455-465,610-619`; `demos/spaceships/src/entity_label.rs:62,72`).
- **Branch on the entity's existing prediction marker** — e.g. `demos/spaceships/src/client.rs:68-79` gives `Interpolated` bullets only a `Sensor` collider (no RigidBody, no Position/Rotation insert), while `Predicted` bullets get the full physics bundle.
- **`DisableRollback` from birth** for local-only entities that must carry Position/Rotation (projectiles, above).
- `PredictionDisable`: **zero uses in examples/demos** (grep). It exists as an internal lightyear marker for predicted-despawn bookkeeping (referenced in the rollback-check query builder at `crates/replication/prediction/src/rollback.rs:193-195`), not an app-facing exclusion tool.

## Cross-cutting: the child-collider timing hole (verified in this clone)

lightyear's avian integration is explicitly aware that child colliders exist and that avian's own child-collider position update is disabled along with `PhysicsTransformPlugin`:
- `crates/integration/avian/src/plugin.rs:853-888` — `update_child_collider_position`: *"Update the child's Position based on the parent's Position and the child's Transform. In avian, this is done in PhysicsSystems::First, so we need to manually run it after PhysicsSystems run"*. It computes `position.0 = rb_pos.0 + rb_rot * collider_transform.translation` for colliders `Without<RigidBody>` with `ColliderOf`.
- In `AvianReplicationMode::Position` (what deterministic_replication uses), that system is scheduled in **`RunFixedMainLoop` / `AfterFixedMainLoop`** (plugin.rs:186-192) — i.e. OUTSIDE the fixed-tick loop.
- Meanwhile `PredictionSystems::UpdateHistory` runs INSIDE the fixed loop, in `FixedPostUpdate` chained after `PhysicsSystems::StepSimulation` (plugin.rs:193-204).

Consequence (framework-level, source-verified): on the very first fixed tick(s) after a child collider entity gains a prediction-participating `Position`/`Rotation` (e.g. via required-component insertion when the collider is added), `update_prediction_history` records whatever value the component holds **before** `update_child_collider_position` has ever run — and with `PhysicsTransformPlugin` disabled, nothing inside the fixed loop initializes it. If that value is avian's placeholder/uninit sentinel, the junk enters `PredictionHistory` permanently (until superseded by a later entry), and any rollback whose target tick maps at-or-before onto that entry restores it (history_buffer.rs:160-168 at-or-before semantics + rollback.rs:964-1010 restore). Additionally, during rollback re-simulation only `FixedMain` is re-run (rollback.rs:1112-1137), so child-collider positions are **never recomputed during rollback ticks** — the parent-relative correction only lands after the rollback completes, in `RunFixedMainLoop` of subsequent frames. UNCERTAIN: whether avian 0.7's required components insert `Position`/`Rotation` PLACEHOLDER sentinels vs finite defaults could not be verified in this clone (avian source absent); our observed literal `Rotation::PLACEHOLDER` values are consistent with the placeholder path.

## Verdict for our bug

The examples establish four invariants that our glb-bind pattern violates; each is independently sufficient to explain why no example ever hits our NaN:

1. **Prediction-registered transform state is never left to required-component defaults on rollback-participating entities.** Every example spawn bundle that will participate in rollback carries an explicit `Position` (and usually `Rotation`) with a real value at the moment the entity/component enters the prediction system (deterministic_replication shared.rs:157/:182; avian_3d_character server.rs:99-101/:180; projectiles shared.rs:1531-1532/:1935-1954). Where physics is deferred, ONLY shape components (`Collider`/`RigidBody`/`LockedAxes`/`Friction`) are deferred — never `Position`/`Rotation`, which are already present via replication (avian_3d_character client.rs:65-79; avian_physics client.rs:41-51,86-102; spaceships client.rs:61-81). Our child colliders instead receive `Position`/`Rotation` via avian's require-insert at collider registration, and — with `PhysicsTransformPlugin` disabled per the integration's own instructions — nothing inside the fixed loop initializes them before `update_prediction_history` records the first (placeholder) value (plugin.rs:186-204, predicted_history.rs:94-123).

2. **`DeterministicPredicted` decoration is atomic with (or after) valid physics state, never before it.** All three deterministic_replication sites and projectiles' `handle_deterministic_spawn` land the marker in the same `spawn`/`insert` as a bundle containing a valid Position (deterministic_replication client.rs:99-109, server.rs:134-148; projectiles client.rs:82-100). Even the one genuinely-deferred activation (`activate_physics_when_bundle_lands`) inserts collider + marker in one atomic command, only after authoritative Position/Rotation landed via the catch-up snapshot. We decorate children whose Position/Rotation are still placeholder-era, in a step disconnected from their initialization.

3. **The framework's built-in grace (`enable_rollback_after`, default 20 ticks → auto-`DisableRollback` during rollbacks reaching into the window, rollback.rs:790-811) protects only against *restore*, not against *recording junk*.** Caveats verified in source: (a) after the window expires, at-or-before lookup (history_buffer.rs:160-168) can still serve a placeholder entry recorded on the bind tick if no later entry superseded it before the rollback fires; (b) a *forced* rollback unconditionally clears `DisableRollback` and drains the protection queue instead of applying it (rollback.rs:781-789); (c) the `on_add` protection silently no-ops if `PredictionResource`/`PredictionManager` doesn't exist at decoration time (rollback.rs:282-291) — worth checking in our app; (d) history entries are recorded every changed tick in FixedPostUpdate regardless of any of this (predicted_history.rs:94-123), so the junk is in the buffer either way.

4. **Nobody puts colliders (or `RayCaster`) on child entities of a predicted body, and nobody binds physics from async assets.** Zero grep hits for `ColliderConstructor`/`ColliderConstructorHierarchy`/`SceneRoot`/`.glb`/`.gltf`, zero physics components inside `with_children` blocks (Q2, Q4). Our structural shape is entirely off the reference map; no example ever needed to answer how `PredictionHistory` behaves for child colliders across a bind-then-rollback sequence.

**Fix shape the examples suggest (in order of fidelity to reference patterns):**

- **(a) Seed before enrolling (primary, directly precedented):** never let the placeholder exist on a history-eligible component. At bind time, compute and insert explicit, finite `Position`/`Rotation` for each child — parent body Position/Rotation composed with the collider's relative transform, exactly the formula in `update_child_collider_position` (plugin.rs:876-886) — **in the same `insert` as the collider components and `DeterministicPredicted`**. This mirrors both the universal spawn-bundle pattern (projectiles shared.rs:1531-1546, deterministic_replication shared.rs:150-166) and the atomic activate-with-state pattern (deterministic_replication client.rs:99-109). Since `PredictionHistory` is created empty on marker+component coexistence (predicted_history.rs:308-310), the first recorded entry will then be real.

- **(b) Keep non-simulated children out of prediction entirely:** if turret/gun/muzzle/wheel collider poses are derived each tick from the body + rig servos, the example-verified pattern for derived/cosmetic Position-carriers is `DisableRollback` from birth (projectiles shared.rs:1542-1545) — don't give them `DeterministicPredicted` at all, and recompute their pose after rollback instead. This removes both the junk-history hazard and per-child rollback cost.

- **(c) If children must be rollback-enrolled and (a) can't be fully atomic:** verify the grace window actually engages (`PredictionResource` present at on_add, rollback.rs:282-291), size `enable_rollback_after` above the worst-case rollback depth at 100ms RTT, and beware forced rollbacks bypassing it (rollback.rs:781-789). But note: no example demonstrates recovering from already-recorded placeholder history — every example achieves safety by preventing the placeholder from ever being recorded. A grace-window-only fix is **UNCERTAIN / not precedented**; (a) remains required for correctness after the window.
